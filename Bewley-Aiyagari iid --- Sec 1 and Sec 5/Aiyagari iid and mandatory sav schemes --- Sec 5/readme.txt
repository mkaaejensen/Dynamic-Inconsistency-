- By default the scripts in this folder loads a precomputed solution and plots
- To replicate, change loadoutput to = 0 and figonly to = 0 and run 
  NOTE: These are very high precision computations for the paper so it is advisable to back up (or create a copy) and change Handle (in order not to 		overwrite) before running files/changing parameters etc.


