- By default the script in this folder loads a precomputed solution and plots/writes to console
- To replicate, change loadoutput to = 0 and figonly to = 0 and run 
  NOTE: This is a very high precision computation for the paper so it is advisable to back up (or create a copy) and change Handle (in order not to 		overwrite) before running files/changing parameters etc.


